# ---- Android / Gradle ----
.gradle/
.kotlin/
build/
local.properties
.cxx/
.externalNativeBuild/
captures/
*.iml
*.apk
*.aab
*.ap_
*.dex
*.class
*.hprof

# ---- IDE ----
.idea/
.vscode/
*.swp
*.swo

# ---- OS ----
.DS_Store
Thumbs.db
desktop.ini
._*

# ---- 第三方二进制 ----
# Windows 版 platform-tools 只在「Pico 重启后走 USB 引导」时用得到,
# 体积 20MB+ 且是上游产物,不入库。需要时跑 tools/fetch-platform-tools.sh
tools/platform-tools/

# ---- 本地私有配置 ----
# 真实设备内网地址等,不入库。模板见 tools/device.env.example
tools/device.env

# ---- 签名密钥(绝对不能入库)----
# 真泄露了等于任何人都能以你的名义发版;丢了则已装机的应用永远无法升级。
# 生成: bash tools/gen-keystore.sh
keystore.properties
*.jks
*.keystore
tools/keystore/

# ---- 发版产物 ----
# tools/release-apk.sh 的输出,APK 属于 GitHub Release 附件,不入库
dist/

# tv-install.sh 的列表位置缓存(本机状态)
tools/.tv-pos-*

# 脚本临时目录(_common.sh 的默认 TMP;自截图、构建日志等落在这里)
.tmp/

# ---- 本地智能体数据(WorkBuddy) ----
# 工作日志 / 记忆等本机数据,且可能含真实设备地址,不入库
.workbuddy/
