# 签名期望值 —— 这个文件**可以**入库,和 keystore.properties 不是一回事。
#
# 里面只有别名和证书 SHA-256 指纹。指纹是公钥的哈希,**不是秘密**:
# 公开它,别人既反推不出私钥,也签不出能被安装的包。
#
# 它的用途是**自检** —— 任何机器拿到一份凭据后,先跑:
#     bash tools/gen-keystore.sh --manifest
# 就能知道「手里这把是不是本仓库发布用的那把」,
# 而不是等发完版才从用户的 INSTALL_FAILED_UPDATE_INCOMPATIBLE 里发现。
#
# ⚠️ 凭据本体(release.jks + 密码)绝对不能入库,存密码管理器。见 docs/06。
#
# 更新:bash tools/gen-keystore.sh --manifest --write
# 自检:bash tools/gen-keystore.sh --manifest
#
# 生成时间:2026-09-18 08:01:49 +0800
# 指纹格式:小写无冒号(与 apksigner 一致)
#
# drill.* 是恢复演练的记录,由 --drill <文件> --record 写入:
#   drill.last    上次演练日期(超过半年 --status 会提醒重跑)
#   drill.where   **那份副本放在哪**(持久位置:飞书/密码管理器/U 盘…)
#   drill.file    演练时本地那个文件叫什么(可能只是临时下载,验完就删)
#   drill.size    字节数 —— 用来说明「验的到底是哪一份」

default_alias=release
alias.release=eb3bfaf6847264a0e696996fb02a91fa7e034ec3c42ef9a85c68168d4d573dbd

drill.last=2026-09-17
drill.where=飞书个人聊天(文件消息)
drill.file=feishu.b64
drill.size=7303
